<h1>Trips</h1>
<ul>
<% @trips.each do |trip| %>
<%# <li><%= link_to trip.destination, user_trip_path(trip.user_id) %> - <%= trip.star_date %> - <%= trip.end_date %></li> %>
<% end %>
</ul>
<%= link_to "New trip", new_user_trip_path if policy(Trip).new? %>
