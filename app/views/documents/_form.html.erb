<div class="container">
    <%= simple_form_for [@itinerary, @document] do |f| %>
      <%= f.input :description %>
      <%= f.input :price %>
      <%= f.input :photo, as: :file %>
      <%= f.button :submit, class: "btn-singup btn btn-ghost mt-2" %>
    <% end %>
</div>
